# DCT numerical audit (Stage A5)

> Generated by  workflow helpers.
> Compares scipy.fft.dctn vs scipy.fftpack.dct on 5 sample images, 8x8 patch DCT-II ortho.

## Question

The repo uses  while the PDF
spec literally says  twice (once per axis). The image-fix
plan calls for verifying these produce identical output.

## Method

For 5 sample images (test_samples/), resize to 224x224 grayscale, split into 8x8
patches, compute DCT-II ortho with both APIs. Report max abs difference per
patch, then overall max across all patches and all images.

## Per-image results

| Image | Max abs diff (across all 8x8 patches) |
|---|---|
| sample1.jpg | 0.000e+00 |
| sample2.jpg | 0.000e+00 |
| sample3.jpg | 0.000e+00 |
| sample4.jpg | 0.000e+00 |
| sample5.jpg | 0.000e+00 |

## Overall

Max abs diff across all 5 images x ~784 patches each: **0.000e+00**

## Verdict

**IDENTICAL (numerically equivalent)**

If overall diff < 1e-9: numerically identical (float64 roundoff only).
If overall diff < 1e-6: identical within float tolerance, safe to use either API.
If overall diff > 1e-6: implementations differ; investigate.

The repo uses scipy.fft.dctn because it was reported as more stable on Windows in
V3 (documented in  history). The
numerical equivalence proven here means the API choice is benign.
