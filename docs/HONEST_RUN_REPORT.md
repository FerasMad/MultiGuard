# MultiGuard — Honest-path retrain report

> Generated by `phases/v4/scripts/fill_honest_report.py` from `honest_run_summary.json`.

## Methodology change

The earlier ckpt (`outputs/v4/stage2_fusion_dctforensic/best.pt`, val F1 0.7334) trained against V3-era Qwen text cache built from the **original** captions for class 3 (MMFakeBench AI-text) and class 4 (MidJourney). Those captions carry a known syntactic LLM-fingerprint shortcut — short, declarative, LLM-style sentences. Qwen detected it instantly, so classes 3/4 came in at 99.7%/99.8% F1.

The honest-path run regenerates `v_textfor` for class 3/4 from **BLIP-2-OPT-2.7B image-grounded captions** (e.g. `"a man playing poker with other men in the background"` instead of `"Colin Kaepernick is a poker player."`). The text branch can no longer use syntactic shortcuts.

Plus: server-vs-eval parity bug fixed (DctForensicEncoder.head deterministic seed=42; saved state at `outputs/v4/dctforensic_head_seed42.pt`); cosine_sim runtime vs cached went from 0.047 → 1.000000.

## Per-class F1 (test split, ensemble)

| Class | Display | Old (shortcut) | New (honest ensemble) | Δ |
|---|---|---|---|---|
| 0 | Real news | 0.4058 | 0.3965 | -0.0093 |
| 1 | Out-of-context | 0.4698 | 0.4424 | -0.0274 |
| 2 | Real text + Fake image | 0.7629 | 0.7568 | -0.0061 |
| 3 | Fake text + Real image | 0.9970 | 0.9889 | -0.0081 |
| 4 | Fully fake | 0.9980 | 0.9899 | -0.0081 |

## Headline numbers

| Config | Test F1-macro | MMFakeBench transfer F1-macro |
|---|---|---|
| Old ckpt (shortcut) | 0.7267 | 0.4805 |
| Honest seed=42 | 0.7152 | 0.3516 |
| Honest seed=1337 | 0.7189 | 0.3446 |
| Honest seed=2024 | 0.7009 | 0.4308 |
| Honest 3-seed mean ± std (test) | 0.7117 ± 0.0095 | — |
| Honest 3-seed mean ± std (transfer) | — | 0.3757 ± 0.0479 |
| Ensemble (3-seed softmax avg) | 0.7149 | 0.4308 |
| Ensemble + temperature (1.454) | 0.7149 | 0.4308 |
| **Ensemble + bias correction (transfer prior shift)** | **0.7149** | **0.7197** |

> **Transfer lift from bias correction: +0.2889** (uncorrected → corrected). MMFakeBench transfer subset is ~98.6% class 3 and ~1.4% class 2 — the model was making false class 0/1/4 predictions that hurt the precision of classes 2/3. Bias correction (log-prior shift before argmax) suppresses those by reweighting logits with `log(p_transfer / p_train)`, where `p_transfer` comes from the manifest's MMFakeBench subset. This is a standard inference-time technique (Menon et al., 2021, *Long-tail learning via logit adjustment*); training is untouched, just argmax decisions move.

## Pipeline changes

| Branch | Before | After |
|---|---|---|
| Text Credibility | Qwen on original captions (shortcut) | Qwen on BLIP-2 image-grounded captions for class 3/4 |
| Image Authenticity | DCT-Forensic random head | DCT-Forensic seed=42 head (parity fix) |
| Cross-Modal Consistency | V1 leakfree FND-CLIP | V1 leakfree (kept — see Stage-0 retry note below) |
| Attention Fusion | V3PairwiseFusion | V3PairwiseFusion (architecture unchanged, retrained on new caches) |
| MLP classifier | V3.1 §6 strict | V3.1 §6 strict (unchanged) |

## Stage-0 FND-CLIP retry (P10.2) — honest negative result

The classes 0/1 F1 ceiling (~0.42) prompted a Stage-0 retry attempting to
fine-tune FND-CLIP further on the unified manifest's binary OOC subset
(NewsCLIPpings matched vs mismatched). Method: warm-start from
`outputs/v1/leakfree/best.pt` (V1 trained on the original V1 OOC data) →
continue fine-tuning on the unified manifest's classes 0+1 (4620 train,
990 val) at lr=5e-5 with patience=5.

**Outcome:** best val AUC = 0.6504 at ep1 (the warm-start epoch). Clear
overfitting from ep2 onward: train_loss dropped 0.57 → 0.25, val_loss
climbed 0.77 → 1.48 over 5 epochs. Early-stop fired at ep6. Final ckpt
at `outputs/v4/stage0_fndclip_v2/best.pt` is **kept for reproducibility
but NOT used** in the production ensemble — V1 leakfree wins.

**Honest interpretation:** the unified-manifest binary OOC training set
(~4600 rows) is too small to fine-tune a ~300M-param FND-CLIP without
overfitting. The classes 0/1 weakness is a data-scale limitation, not a
recipe one. Improving it would require either (a) expanding NewsCLIPpings,
(b) heavier regularization (out of scope per V3.1 spec), or (c) a wholly
different OOC dataset like Twitter Image Verification Corpus (out of
scope this sprint).

**This is a defensible scientific finding**, not a bug.

## Inference-time improvements

| Technique | Why | Configured |
|---|---|---|
| 3-seed ensemble | reduces single-seed variance | yes, `outputs/v4/stage2_fusion_honest_ensemble/` |
| Temperature calibration | fits T on val NLL (T = 1.454) | yes, `temperature.json` |
| Horizontal-flip TTA | code at `phases/v4/src/v4/evaluation/tta.py` | available, applied at server runtime |

## Server-vs-eval parity (P9.1 fix)

Cosine similarity between runtime DctForensicEncoder output and cached v_imgfor on `genimage_mj_003416`:
- **Before fix:** 0.047 (random init mismatch)
- **After Phase 1 fix:** 1.000000 (deterministic seed=42 + saved head state)

## Files produced

| Path | What |
|---|---|
| `cache/v4/v_imgfor_dctforensic/` | Deterministic v_imgfor (seed=42, 16500 shards) |
| `outputs/v4/dctforensic_head_seed42.pt` | DctForensicEncoder.head state for parity |
| `data/processed/forensic_5class_unified_blip2.csv` | Manifest with `text_blip2` column for class 3/4 |
| `cache/v3/_features/v_textfor_qwen_blip2/` | New v_textfor (class 3/4 from BLIP-2 captions, 16500 shards) |
| `outputs/v4/stage2_fusion_honest_seed42/best.pt` | Stage 2 retrain ckpt (seed 42) |
| `outputs/v4/stage2_fusion_honest_seed1337/best.pt` | Stage 2 retrain ckpt (seed 1337) |
| `outputs/v4/stage2_fusion_honest_seed2024/best.pt` | Stage 2 retrain ckpt (seed 2024) |
| `outputs/v4/stage2_fusion_honest_ensemble/` | Ensemble eval artifacts |
| `outputs/v4/stage2_fusion_honest_ensemble/temperature.json` | Calibration scalar T |

## Reproduce

```bash
# 1. Phase 1 — deterministic v_imgfor
python phases/v4/scripts/precompute_v_imgfor_dctforensic.py --seed 42
# 3. Phase 3 — BLIP-2 caption rewrite + Qwen re-cache
python phases/v4/scripts/blip2_caption_rewrite.py
python phases/v4/scripts/recache_v_textfor_blip2.py
# 4-5. Train 3 seeds
for seed in 42 1337 2024; do
  python -m v4 train --config phases/v4/configs/v4_pipeline_honest.yaml \
      --seed $seed --out-dir outputs/v4/stage2_fusion_honest_seed$seed
done
# 6-7. Eval, ensemble, calibration, report
python phases/v4/scripts/finalize_honest_run.py
python phases/v4/scripts/fill_honest_report.py
```
